# Secrets
.env
.env.*
!.env.example

# Data — never in repo
*.duckdb
*.duckdb.wal
*.csv
*.parquet
*.jsonl
data/

# PII — partner feedback & reply verbatims hold real lead emails. Never push
# (public repo; same identity-risk rule as the domain list). Local/droplet only.
*.xlsx

# Seed data — vendor/cost/partner reference data + sheet snapshots. Out-of-band,
# local/droplet only; never committed to the public repo.
seed_data/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
.python-version
*.egg-info/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Logs
logs/
*.log

# Box-local edit/deploy artifacts (never commit; result of direct-on-box edits).
# Added 2026-06-17 as part of the warehouse deploy-discipline conversion.
*.bak
*.bak-*
*.bak.*
*.bak2-*
*.staged-*
*.stale-*
*.pre-*.bak
.staged-*
_deploy_tmp/
backups/

# Drift-guard machine state (warehouse_drift_guard.sh). Not source.
.drift_baseline

# email-thread-sync per-run rollback manifests (entities/email_thread_sync.py writes these into
# core/ for G7). Generated artifacts, not source — keep them from dirtying the box working tree
# (an untracked file there blocks moderator apply-now's clean-check + trips the drift guard).
core/email_thread_manifest_*.txt
